{
  "CN": "kubernetes",
  "hosts": [
  {% for host in groups['etcd-nodes'] %}
    "{{host}}",
  {% endfor %}
  {% for host in groups['kube-master-nodes'] %}
    "{{host}}",
  {% endfor %}
  {% for host in groups['worker-nodes'] %}
    "{{host}}",
  {% endfor %}
  "127.0.0.1"
  ],
  "key": {
    "algo": "rsa",
    "size": 2048
  },
  "names": [
    {
      "C": "US",
      "L": "Portland",
      "O": "Kubernetes",
      "OU": "Cluster",
      "ST": "Oregon"
    }
  ]
}
